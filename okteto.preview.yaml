deploy:
  commands:
    - name: Generating postgres password...
      command: echo "PGPASSWORD=$(openssl rand -hex 16)" >> "$OKTETO_ENV"
    - name: Deploying compose...
      command: okteto stack deploy -f docker/docker-compose.preview.yml