{
    "properties": {
        "chartType": {
            "type": "string",
            "enum": ["big_number", "table", "cartesian"]
        },
        "config": {
            "type": "object",
            "nullable": true,
            "additionalProperties": true
        }
    }
}
