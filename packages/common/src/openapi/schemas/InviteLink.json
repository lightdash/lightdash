{
    "type": "object",
    "properties": {
        "expiresAt": {
            "type": "string",
            "format": "date-time"
        },
        "inviteCode": {
            "type": "string"
        }
    },
    "required": ["expiresAt", "inviteCode"]
}
