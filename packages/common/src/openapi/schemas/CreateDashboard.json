{
    "type": "object",
    "properties": {
        "spaceUuid": {
            "type": "string",
            "format": "uuid",
            "description": "The UUID of the space to which the dashboard belongs."
        },
        "name": {
            "type": "string"
        },
        "description": {
            "type": "string",
            "nullable": true
        },
        "tiles": {
            "type": "array",
            "items": {
                "$ref": "./CreateDashboardTile.json"
            }
        },
        "filters": {
            "$ref": "./DashboardFilters.json"
        },
        "updatedByUser": {
            "type": "object",
            "properties": {
                "userUuid": {
                    "type": "string",
                    "format": "uuid"
                }
            }
        }
    },
    "required": ["name", "tiles"]
}
