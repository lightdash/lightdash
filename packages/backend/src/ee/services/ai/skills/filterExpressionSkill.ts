import { MCP_FILTER_EXPRESSION_GUIDANCE_SECTION } from '../prompts/filterGuidance';

export const FILTER_EXPRESSION_SKILL = `---
name: filter-expressions
description: Author filter expressions for run_metric_query and search_field_values, including conditional custom metric filters.
availability: [mcp]
---

<!-- Generated by pnpm -F backend generate:filter-expression-skill. -->

Read this before writing filters for expression-enabled tools. It does not apply to tools whose schemas use structured filter objects. Follow each tool's input schema; this skill does not enable unavailable tools or filter modes.

${MCP_FILTER_EXPRESSION_GUIDANCE_SECTION}
`;
