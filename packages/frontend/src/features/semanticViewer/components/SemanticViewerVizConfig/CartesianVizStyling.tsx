import { CartesianChartDisplayConfig } from '../../../../components/DataViz/config/CartesianChartDisplayConfig';

// NOTE: This file is included here, but the styling is shared from the
// data viz folder for now. If the styling diverges, we can add it here.
export { CartesianChartDisplayConfig as CartesianVizStyling };
