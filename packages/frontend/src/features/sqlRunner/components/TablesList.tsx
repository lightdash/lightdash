import { UnstyledButton } from '@mantine/core';

export const TablesList = () => {
    return (
        <>
            <UnstyledButton>
                <div>Table1</div>
            </UnstyledButton>
        </>
    );
};
