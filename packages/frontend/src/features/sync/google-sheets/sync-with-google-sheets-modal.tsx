import { Modal } from '@mantine/core';

export const SyncWithGoogleSheetsModal = () => {
    return (
        <Modal opened onClose={() => {}}>
            <h1>Sync with Google Sheets</h1>
        </Modal>
    );
};
