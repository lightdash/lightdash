import { Text } from '@mantine-8/core';
import type { FC } from 'react';

export const ListExploresToolCallDescription: FC = () => {
    return (
        <Text c="dimmed" size="xs">
            Listed available explores
        </Text>
    );
};
