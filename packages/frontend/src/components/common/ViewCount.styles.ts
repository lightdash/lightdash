import { Colors, Tag } from '@blueprintjs/core';
import styled from 'styled-components';

export const ViewCountTag = styled(Tag)`
    color: ${Colors.DARK_GRAY5} !important;

    svg: {
        stroke: ${Colors.DARK_GRAY5} !important;
    }
`;
