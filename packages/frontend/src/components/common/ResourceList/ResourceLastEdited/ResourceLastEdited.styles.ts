import { Colors } from '@blueprintjs/core';
import styled from 'styled-components';

export const ResourceLastEditedTimeAgo = styled.div`
    font-size: 12px;
    font-weight: 500;
    color: ${Colors.DARK_GRAY1};
`;

export const ResourceLastEditedBy = styled.div`
    font-size: 12px;
    font-weight: 400;
    color: ${Colors.GRAY2};
`;
