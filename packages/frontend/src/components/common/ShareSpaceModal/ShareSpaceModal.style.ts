import { Button, Card, H5 } from '@blueprintjs/core';
import styled from 'styled-components';

export const ShareSpaceButton = styled(Button)`
    margin-right: 10px;
`;
export const AccessModeWrapper = styled.div`
    display: flex;
    flex: 1 auto 1;
`;
