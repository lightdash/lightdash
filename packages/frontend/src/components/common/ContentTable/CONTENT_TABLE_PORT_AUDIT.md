# Content Table Port Audit

Source of truth: `origin/main` MRT call sites, plus local `mantine-react-table@1.3.4` for display-column behavior. MRT puts row selection in the leading display columns even when row actions are last.

## Shared Compatibility

| Behavior                               | Port Status                                                                                                                                         |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Row selection column                   | Content table now places selection before data columns, matching MRT.                                                                               |
| Row selection checkbox sizing          | Selection cells remove generic data padding so 20px display columns do not crop checkboxes.                                                         |
| Row actions column                     | Supported via `enableRowActions`, `renderRowActions`, and `positionActionsColumn`.                                                                  |
| Hover row background                   | Supported by `mantineTableProps.highlightOnHover`.                                                                                                  |
| Hover-only explore actions             | Ported to CSS for `.explore-button-container` inside table cells.                                                                                   |
| Row click cursor                       | Supported when a row has `onClick`, or via explicit row `style.cursor`.                                                                             |
| Selected row background from row props | Row `style.backgroundColor` is propagated to cells.                                                                                                 |
| Cell editing                           | Supported for `editDisplayMode: 'cell'` through `Edit`, `editingCell`, and `setEditingCell`.                                                        |
| Virtualized infinite scroll            | Uses TanStack virtualizer estimates, measured rows, overscan, and resets scroll to row 0 when sorting/search changes.                               |
| Performant column resizing             | Uses TanStack's CSS variable sizing pattern and memoizes body rows during resize drags.                                                             |
| Resize bounds                          | Data columns use MRT-compatible defaults plus a derived minimum from configured `size`; row-select display columns clamp to a Mantine 8-safe width. |
| Loading skeletons                      | Skeleton rows inherit the table's virtual row estimate so tall catalog/resource rows do not render short placeholders.                              |
| Filtered empty states                  | Base table can render filtered empty copy with `Clear all filters` via `emptyState`.                                                                |
| Simple outer `sx`                      | Converted for paper/container props only.                                                                                                           |
| Structural `sx` selectors              | Must be migrated to CSS or explicit props. Do not re-enable table/row/cell layout `sx` blindly.                                                     |

## Tables

| Table                                        | Old Features                                                                                                                                 | Port Notes                                                                |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `CompilationHistoryTable`                    | Virtualized infinite table, no pagination, top toolbar, row click, loading/progress, fixed container height.                                 | Row click/cursor and virtual scroll supported.                            |
| `PreAggregateStatsTable`                     | Paginated table, top toolbar, custom compact head/body sizing.                                                                               | Pagination and custom cell styles supported.                              |
| `PreAggregateMaterializations`               | Paginated table, top toolbar, row click, hover rows, max-height container.                                                                   | Row click/cursor and hover supported.                                     |
| `ProjectAccess`                              | Paginated access table, top toolbar, non-virtualized, custom head/body sizing.                                                               | Pagination and toolbar supported.                                         |
| `LogsTable`                                  | Virtualized infinite logs, no pagination, top toolbar, row click, progress state.                                                            | Row click/cursor and virtual scroll supported.                            |
| `SchedulersTable`                            | Virtualized table, conditional row selection, top toolbar and bulk actions, no pagination.                                                   | Selection now leads on the left like MRT.                                 |
| `ValidatorTable`                             | Virtualized table, pinned row class, top toolbar, loading/error states.                                                                      | Row classes supported.                                                    |
| `MyAppsPanel`                                | Non-paginated settings table, right-aligned actions, custom empty text.                                                                      | Alignment props supported.                                                |
| `ProjectManagementPanel`                     | Non-paginated project table, expanding search, top toolbar filters/modals, filtered empty state, right-aligned action cells.                 | Search and filtered empty state now use shared content table primitives.  |
| `UserAttributesPanel`                        | Non-paginated table, top toolbar, custom empty states, compact sizing.                                                                       | Toolbar and empty states supported.                                       |
| `GroupsTable`                                | Virtualized users/groups table, top toolbar, compact sizing.                                                                                 | Virtual scroll and toolbar supported.                                     |
| `UsersTable`                                 | Virtualized users table, top toolbar, compact sizing.                                                                                        | Virtual scroll and toolbar supported.                                     |
| `VerifiedContentPanel`                       | Non-paginated table, row selection disabled, compact verified-content cells.                                                                 | Disabled selection and cell styles supported.                             |
| `InfiniteResourceTable`                      | Virtualized infinite content table, optional row selection, row actions, hover-only explore action, top and bottom toolbars, cell edit mode. | Selection leading, hover explore, row actions, and cell editing ported.   |
| `AiAgentAdminAgentsTable`                    | Finite agents table, top toolbar filters/count, custom actions menu in last column, no pagination.                                           | Removed forced 600px min-height for one-row view.                         |
| `AiAgentAdminThreadsTable`                   | Virtualized infinite threads, top/bottom toolbar filters, row click, selected-thread background.                                             | Selected row background ported from row `sx` to real row style.           |
| `VerifiedArtifactsTable`                     | Artifact list, sticky header, bottom toolbar, selected artifact row background, row click.                                                   | Row background propagation supports selected artifact.                    |
| `EvalRunDetails`                             | Sticky table, row click when thread exists, selected-thread background, no toolbar.                                                          | Row background propagation supports selected result.                      |
| `EvalRuns`                                   | Sticky runs table, row click, no toolbar.                                                                                                    | Row click/cursor supported.                                               |
| `ServiceAccountsTable`                       | Non-paginated service accounts table, top toolbar, right-aligned action columns.                                                             | Alignment and toolbar supported.                                          |
| `ProjectChangesets`                          | Non-paginated changes table, bottom toolbar, trailing row action button, hover rows.                                                         | Row actions and hover supported.                                          |
| `MetricsTable`                               | Virtualized infinite metrics table, top/bottom toolbar filters, hover-only Explore button, description overlay, editable category cell.      | Explore hover, description overlay, and editable category cell ported.    |
| `ProjectGroupAccess`                         | Paginated group access table, top toolbar, non-virtualized.                                                                                  | Pagination and toolbar supported.                                         |
| `RecentlyDeletedPage`                        | Virtualized table, top toolbar filters, restore/delete interactions, no pagination.                                                          | Virtual scroll and toolbar supported.                                     |
| `DashboardVersionComparison` dashboard tiles | Virtualized comparison table, compact top toolbar, tiny text sizing.                                                                         | Compact sizing supported.                                                 |
| `DashboardVersionComparison` filters         | Virtualized comparison table, compact top toolbar, tiny text sizing.                                                                         | Compact sizing supported.                                                 |
| `DashboardVersionComparison` parameters      | Virtualized comparison table, compact top toolbar, tiny text sizing.                                                                         | Compact sizing supported.                                                 |
