import styled from 'styled-components';

export const Wrapper = styled.div`
    height: 60px;
    display: flex;
    flex-direction: row-reverse;
    padding-top: 10px;

    button {
        margin: 0;
    }
`;
