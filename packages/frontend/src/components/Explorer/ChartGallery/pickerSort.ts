import { type DataAppVizListSort } from '@lightdash/common';

/** Project chart types are listed A–Z so server pages append in place. */
export const PICKER_SORT: DataAppVizListSort = {
    sortBy: 'name',
    sortDirection: 'asc',
};
