{
    // This file will extend the tsconfig.json file for the test environment
    // This means we will not apply the noUncheckedIndexedAccess option to the test files
    // Change build method in package.json to use this file
     // It is temporary disabled in package.json until we fix all the errors
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "noUncheckedIndexedAccess": true
    },
    "include": [
        "src/**/*.ts",
        "src/**/*.json"
    ],
    "exclude": [
        "src/**/*.test.ts",
        "src/**/*.spec.ts",
        "src/**/*.mock.ts",
        "src/__mocks__/**/*.ts"
    ]
} 