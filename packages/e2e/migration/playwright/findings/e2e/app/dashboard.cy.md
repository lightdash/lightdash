# packages/e2e/cypress/e2e/app/dashboard.cy.ts

## Classification

Recommended runner: Playwright for the active/UI behavior; API tests for the skipped API-only slug check and, after scope clarification, export orchestration
Execution lane: Serial mutable-project lane
Active tests: 3
Skipped tests: 6
Persistent mutation: Yes — the active creation test creates one dashboard and three dashboard-owned charts, then repeatedly updates the dashboard without cleanup (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:140-349`)
Shared-preview dual-run safe: No — Cypress and Playwright would create the same `Title` dashboard and fixed-name charts in the same seed project (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:140-149`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:184-190`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:236-242`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:291-297`)
Difficulty total: 13/18 (persistent/shared state 3, browser interaction complexity 3, environment/external dependencies 2, synchronization/flakiness 3, authentication/authorization 1, cross-file infrastructure 1)
Coordination keys: `project:3675b69e-8324-4110-bdca-059031aa8da3`, `dashboard:jaffle-dashboard`, `dashboard-name:Title`, `dashboard-name:Small`, `chart-name:What's the number of unique payments per payment method?`, `chart-name:What's the total revenue per payment method?`, `chart-name:Stg Payments (payment method x amount)?`, `sessionStorage:dashboardUuid/fromDashboard/unsavedDashboardTiles:*`, `scheduler:EXPORT_CONTENT`
Analysis status: coordination-required

## Test inventory

| Title | Effective status | Behavior | Mutations | Unusual mechanics | Recommended target |
|---|---|---|---|---|---|
| `Should see dashboard` | Active | Opens the dashboard list, selects seeded `Jaffle dashboard`, checks two chart titles, absence of loading/error states, three ECharts wrappers, the big-number label, and six table headers (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:8-36`). | No intended persistent mutation; reading the dashboard records a view asynchronously in application code (`packages/backend/src/services/DashboardService/DashboardService.ts:570-614`). | Deferred below-fold rendering is triggered with two animated scroll operations; it relies on CSS implementation classes and exact chart/header counts (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:16-35`). | Playwright UI test in `packages/e2e/playwright/app/dashboard.spec.ts`. |
| `Should use dashboard filters, should clear them for new dashboards` | Skipped via `it.skip` | Adds a temporary `payment_method = credit_card` filter, checks URL serialization and reload restoration, then creates a new dashboard and expects all filter query parameters to be cleared (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:39-101`). | Would create a fixed-name `Title` dashboard and does not delete it (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:89-100`). The filter before creation is temporary URL state, not a saved dashboard update (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:76-87`). | Fixed 200 ms debounce wait, Mantine combobox, forced Apply, URL-encoded JSON state, reload, and cross-page state reset (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:55-100`). | Playwright, not a pure unit/API test: URL persistence across a real reload and clearing during UI navigation are browser behaviors. Clarify the stale `todo: move to unit tests` before reviving (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:38-39`). |
| `views underlying data with dimensions and metrics` | Active | Opens the first big-number tile's menu and verifies that the underlying-data dialog offers two metrics and three dimensions (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:103-138`). | No persistent content mutation; opening the dashboard still records a view. | Scrolls/clicks a generic first big-number value and force-clicks a visible portal menu item because hidden Mantine menus remain mounted (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:117-126`). | Playwright UI test in `packages/e2e/playwright/app/dashboard.spec.ts`. |
| `Should create dashboard with saved chart + charts within dashboard + filters + tile targets` | Active | Creates a dashboard; adds one existing saved chart; creates three charts from two explores; adds/persists a default filter; changes tile targeting; verifies cross-explore target matching; adds Markdown; and checks final chart success states (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:140-349`). | Creates fixed `Title` dashboard, three dashboard-owned saved charts, a Markdown tile, dashboard versions, and saved filter/target changes. The existing seeded saved chart is linked, not modified. There is no teardown (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:140-349`). | Long multi-route workflow; Mantine portals; many force clicks; `sessionStorage` handoff; field-value queries; fixed 200 ms wait; index/count-based tabs and checkboxes; cross-explore automatic field matching; exact toast text (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:151-348`). | One Playwright UI test in `packages/e2e/playwright/app/dashboard.spec.ts`; preserve the intentional within-test state progression, but use unique names and API cleanup. |
| `Should preview a dashboard image export` | Skipped via `it.skip` | Creates a small dashboard/chart, saves it, opens export, generates an image preview, and waits for a generic Success message (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:352-387`). | Would create fixed `Small` dashboard/chart plus an export job and generated image; no cleanup (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:352-386`). | Clicks all matching Add-tile elements, sleeps 2 seconds, and allows 20 seconds for an async scheduler/headless-browser result (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:360-386`). | Clarify intended scope. If the contract is export job success, move to `packages/api-tests/tests/dashboard.test.ts`; if export-modal UX is required, use Playwright and provision scheduler, headless browser, and file storage. Do not copy as an unexplained skip. |
| `Should access dashboard by slug instead of UUID` | Skipped via `it.skip` | Visits seeded `jaffle-dashboard`, checks two titles, slug retention, chart completion, and three ECharts wrappers (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:390-408`). | No intended content mutation; dashboard view tracking still occurs. | Direct slug route plus visualization/deferred-render assertions. | Playwright UI test in `packages/e2e/playwright/app/dashboard.spec.ts`; the rendered-chart assertions genuinely require a browser despite the unit/API todo (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:389-390`). |
| `Should maintain dashboard filters when using slug` | Skipped via `it.skip` | Adds a temporary filter on the slug route, verifies slug and `tempFilters` coexist, reloads, and checks both persist (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:411-450`). | URL-only temporary filter state; no saved dashboard update. | Fixed 200 ms wait, Mantine combobox, forced Apply, reload, and URL state. | Playwright UI test in `packages/e2e/playwright/app/dashboard.spec.ts`; this is browser routing/state behavior, not API-only behavior. |
| `Should access dashboard via API using slug` | Skipped via `it.skip` | Sends `GET /api/v1/dashboards/jaffle-dashboard` and asserts response status, name, slug, UUID, and tiles (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:453-471`). | Read-only apart from any service-side view tracking. | Entirely API-only and uses an apparently stale unscoped v1 URL. Current frontend retrieval is project-scoped v2 (`packages/frontend/src/hooks/dashboard/useDashboard.ts:37-44`), and the current v2 controller exposes that route (`packages/backend/src/controllers/v2/ProjectDashboardController.ts:29-62`). | API test in `packages/api-tests/tests/dashboard.test.ts`, updated to the intended current endpoint after clarification; otherwise remove as stale. |
| `Should handle invalid dashboard slug gracefully` | Skipped via `it.skip` | Visits a nonexistent dashboard slug and expects rendered `Dashboard not found` text (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:474-482`). | None. | Uses `failOnStatusCode: false`; that option concerns Cypress navigation semantics rather than the application's API request. The model throws exactly `Dashboard not found` (`packages/backend/src/models/DashboardModel/DashboardModel.ts:887-927`). | Playwright UI test if the rendered error state is product behavior; API test only if the desired contract is the HTTP 404. Clarify before reviving. |

The suite has no inherited skipped `describe`: `describe('Dashboard')` is active (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:3`). Therefore the effective totals are three active and six skipped tests.

## Cypress command expansion

- The only project-defined Cypress command invoked directly is `cy.login()` from the suite-wide `beforeEach` (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:3-6`). Its implementation uses `cy.session` keyed by the seed admin email, performs `POST api/v1/login` with seed admin credentials, requires status 200, and validates restored sessions with `GET api/v1/user` status 200 (`packages/e2e/cypress/support/commands.ts:152-173`).
- The credentials are `demo@lightdash.com` / `demo_password!`, and the seed user's organization role is ADMIN (`packages/common/src/index.ts:474-481`). Thus every test runs as an organization admin, not an editor/viewer, and creation, update, filter editing, underlying-data, and export permission paths are not tested for lower roles.
- `findByText`, `findAllByText`, `findByRole`, `findAllByRole`, `findByLabelText`, `findByPlaceholderText`, and `findByTestId` are Testing Library's Cypress commands, not project-defined behavior. The command module loads `@testing-library/cypress/add-commands` (`packages/e2e/cypress/support/commands.ts:46`), and the E2E support file imports that module globally (`packages/e2e/cypress/support/e2e.ts:14-16`).
- All other calls (`visit`, `contains`, `get`, `within`, `scrollIntoView`, `window`, `url`, `reload`, `request`, `wait`, and element actions/assertions) are Cypress built-ins. There are no command aliases, `cy.intercept` routes, fixture aliases, tasks, uploads, or downloads in this source.
- Playwright already provides equivalent authenticated storage state: setup posts the same credentials, validates `/api/v1/user`, and writes the request context storage state (`packages/e2e/playwright/auth.setup.ts:1-25`); the Firefox project consumes it (`packages/e2e/playwright.config.ts:31-49`). No dashboard-specific shared authentication helper is needed.

## State, seed, and environment assumptions

- Every route uses seed project UUID `3675b69e-8324-4110-bdca-059031aa8da3`, named `Jaffle shop` (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:1`, `packages/common/src/index.ts:550-558`).
- The suite assumes a seeded dashboard named `Jaffle dashboard` with slug `jaffle-dashboard`. The seed creates it with five saved-chart tiles — revenue by payment method, total revenue, orders over time, average spend, and a customer table — plus Markdown/Loom content (`packages/backend/src/database/seeds/development/03_saved_dashboards.ts:113-213`, `packages/backend/src/database/seeds/development/03_saved_dashboards.ts:244`). The first UI test's count of three `.echarts-for-react` nodes is therefore not the total chart-tile count; it assumes the other two visualizations are a big number and table.
- The seeded dashboard already has two saved dimension filters (`orders_is_completed = true` and completed past 10 years) (`packages/backend/src/database/seeds/development/03_saved_dashboards.ts:203-243`). The skipped temporary-filter test's expectation that the URL lacks `filters=`/`years` is about URL overrides, not absence of saved filters (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:76-82`).
- The saved chart `How much revenue do we have per payment method?` is seeded on explore/table `payments`, dimensions by `payments_payment_method`, and includes total-revenue and unique-payment metrics (`packages/backend/src/database/seeds/development/02_saved_queries.ts:36-102`). The big-number chart is also seeded from `payments` and labels itself `Payments total revenue` (`packages/backend/src/database/seeds/development/02_saved_queries.ts:111-155`). These definitions underpin `credit_card`, `bank_transfer`, metric labels, ECharts counts, and tile-target assumptions.
- Dashboard list retrieval is `GET /api/v1/projects/{projectUuid}/dashboards?...` (`packages/frontend/src/hooks/dashboard/useDashboards.ts:8-17`). Dashboard detail/slug retrieval is project-scoped `GET /api/v2/projects/{projectUuid}/dashboards/{uuidOrSlug}` (`packages/frontend/src/hooks/dashboard/useDashboard.ts:37-44`). Chart tiles execute `POST /api/v2/projects/{projectUuid}/query/dashboard-chart` (`packages/frontend/src/hooks/dashboard/useDashboardChartReadyQuery.ts:26-38`). Filter value search executes `POST /api/v2/projects/{projectUuid}/query/field-values` and polls `GET /api/v2/projects/{projectUuid}/query/{queryUuid}` with exponential backoff (`packages/frontend/src/hooks/useFieldValues.ts:158-225`). Underlying data similarly posts to `/query/underlying-data` and polls paged query results (`packages/frontend/src/hooks/useUnderlyingDataResults.ts:27-78`). A reachable backend, database, cached explores, and demo warehouse/query execution are required.
- Dashboard creation is `POST /api/v1/projects/{projectUuid}/dashboards`; updates are `PATCH /api/v2/projects/{projectUuid}/dashboards/{id}` (`packages/frontend/src/hooks/dashboard/useDashboard.ts:46-84`). Dashboard-owned chart creation is `POST /api/v1/projects/{projectUuid}/saved` (`packages/frontend/src/hooks/useSavedQuery.ts:27-50`). The active mutation test therefore writes real shared database rows/versions rather than browser-only drafts.
- Dashboard creation chooses the first root space when no explicit default is supplied, and only offers spaces where the current user can create a Dashboard (`packages/frontend/src/components/common/modal/DashboardCreateModal.tsx:65-107`). The seed admin must retain create access to at least one root space.
- Creating a chart from inside a dashboard persists the chart immediately, stages the new tile in `sessionStorage`, and navigates back to edit mode (`packages/frontend/src/components/common/modal/ChartCreateModal/SaveToSpaceOrDashboard.tsx:357-414`). Keys include `fromDashboard`, `dashboardUuid`, and dashboard-specific unsaved tiles/filters/tabs (`packages/frontend/src/hooks/dashboard/useDashboardStorage.ts:8-23`, `packages/frontend/src/hooks/dashboard/useDashboardStorage.ts:87-176`). The port must preserve one browser context/page through each chart-creation redirect.
- Temporary filters are compressed into the `tempFilters` URL parameter and restored via `JSON.parse` on mount (`packages/frontend/src/providers/Dashboard/DashboardProvider.tsx:1086-1129`, `packages/frontend/src/providers/Dashboard/DashboardProvider.tsx:1218-1262`). Saved-filter overrides use a separate `filters` parameter.
- Cypress uses `http://localhost:3000`, 1920x1080, 10-second default command timeout, and two run-mode retries by default (`packages/e2e/cypress.config.ts:11-35`). Playwright defaults to the same base URL and viewport but currently only runs Desktop Firefox, one worker, no local retries, 10-second action/assertion timeouts, and 30-second navigation timeout (`packages/e2e/playwright.config.ts:4-49`). ECharts and Mantine behavior must be validated in Firefox rather than assuming Cypress's browser behavior.
- The skipped image preview is materially more demanding: it posts `POST /api/v2/dashboards/{uuid}/exports`, polls `/api/v1/schedulers/job/{jobId}/status` every two seconds, and expects a returned file URL (`packages/frontend/src/hooks/dashboard/useDashboard.ts:220-236`, `packages/frontend/src/hooks/dashboard/useDashboard.ts:331-390`, `packages/frontend/src/features/scheduler/hooks/useScheduler.ts:194-203`, `packages/frontend/src/features/scheduler/hooks/useScheduler.ts:480-513`). The backend schedules `EXPORT_CONTENT` (`packages/backend/src/services/DashboardService/DashboardService.ts:162-245`), a scheduler worker executes it (`packages/backend/src/scheduler/SchedulerWorker.ts:1094-1127`), and screenshot capture fails without `HEADLESS_BROWSER_HOST` (`packages/backend/src/services/UnfurlService/UnfurlService.ts:1162-1174`). This is not safe to classify as a simple frontend unit test.
- No source-level environment variables, timezone assumptions, clipboard, popup, iframe, Monaco, upload, or direct file-download assertions are present. Export preview indirectly involves a headless browser and generated image storage.

## Synchronization and timeout requirements

- No request aliases are used. Read tests wait on disappearance of `Loading dashboards`/`Loading chart`; the creation test instead waits on visible controls, route substrings, toasts, and final absence of loading/error messages (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:11-24`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:192-219`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:346-348`). A Playwright port should wait for user-visible readiness or the specific query responses, not `networkidle`, because query polling/background traffic may continue.
- The first test's 500 ms animated element scroll followed by 500 ms window scroll exists to trigger deferred below-fold chart rendering (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:26-30`). Replace animation durations with `scrollIntoViewIfNeeded()` on the below-fold chart/table and assert visibility/readiness; retain a viewport of 1920x1080.
- Filter field search uses hard sleeps of 200 ms in three places (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:58-66`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:197-204`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:424-431`). Playwright should wait for the exact option to become visible/enabled, which naturally covers debounce and field-value polling.
- The create test grants 30 seconds to find `Create dashboard` and 10 seconds around modal creation (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:143-149`). Use a test-local extended timeout for the full multi-query workflow and targeted response/visibility waits; do not raise global timeouts.
- Each `Save chart` causes persisted chart creation, session-storage tile staging, navigation away, then redirect to `/dashboards` (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:172-193`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:224-245`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:278-300`). Start URL waits before clicking Save to avoid missing a fast navigation, then wait for edit-mode controls and chart completion before the next action.
- Checkbox counts 4 then 5 encode the exact number of charts plus Select all (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:250-258`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:302-307`). If retained, assert named tile rows and their checked state; count only within the visible tiles panel.
- The skipped export test's unconditional 2-second sleep plus 20-second Success allowance is weaker than the implementation's scheduler polling and screenshot timeouts (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:379-386`). A revived test needs a substantially larger test-local timeout and should wait for the specific preview success toast or job result, not a generic `div` containing `Success`.
- Cypress run retries default to two while local Playwright retries are zero (`packages/e2e/cypress.config.ts:11-21`, `packages/e2e/playwright.config.ts:4-15`). The port must remove fixed waits/ambiguous locators rather than relying on retry parity.

## Locator and strictness risks

- CSS implementation selectors `.react-grid-layout`, `.react-grid-item`, `.echarts-for-react`, `.mantine-MultiSelect-input`, and raw `thead th` are used throughout (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:16-35`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:166`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:268-269`). Prefer dashboard/tile roles, titles, test IDs, and visible scoped content. Keep `.echarts-for-react` only if no semantic readiness marker exists; it checks wrapper count, not pixels/canvas content.
- Playwright locators are strict. Generic text actions such as `Payments`, `Payment method`, `Save`, `Add`, `Title`, `Create`, and `Apply` can match multiple visible or retained nodes (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:148-187`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:333-342`). Scope them to the active dialog/form/menu and use exact accessible roles/names.
- The source explicitly documents two Mantine v8 portal hazards: duplicate Add-tile triggers can toggle open then closed, and hidden dropdowns persist in the DOM (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:121-126`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:151-157`). Port with the visible first Add-tile button and visible role-based menu item; do not reproduce `{ force: true }` unless a real hidden-input control demands it.
- `first()`, `last()`, and numeric `eq()` encode layout rather than identity: first big number, first saved-chart tile, last cross-explore tile, first/second tab, and checkbox indexes (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:117-120`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:250-269`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:304-331`). Scope by tile/chart name and tab accessible name. The test already demonstrates a safer named tile-row pattern at lines 311-325.
- `cy.findByRole('dialog').get('.mantine-MultiSelect-input')` does not express an unambiguous descendant relationship for the port (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:158-167`). Use `dialog.locator(...)` or close via Escape/selected option state.
- Assertions such as `contains('bank_transfer').should('have.length', 0/1)` search broad DOM text and can be affected by hidden portal content or another tile (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:213-214`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:267-269`). Scope to named chart tiles and visible text.
- The API-only skipped test uses Chai property assertions on an untyped response (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:457-470`). An API-test port should parse/validate the response using repository response types/guards rather than unsafe casts.

## Nonstandard or surprising behavior

- The active creation test is one intentional state machine, not independent cases: the first chart/filter must be saved before creating the second chart; the tile-target change must be saved before creating the cross-explore chart (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:216-245`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:250-300`). Splitting it without API setup would destroy the behavior it is checking.
- Dashboard-owned chart rows are created before `Save changes`; only the tile association is staged in `sessionStorage` (`packages/frontend/src/components/common/modal/ChartCreateModal/SaveToSpaceOrDashboard.tsx:357-414`). A failure midway can leak charts/dashboard state even if the final dashboard save never occurs.
- The cross-explore checkbox automatically selects a target using exact match, then field type/name, then field type fallback (`packages/frontend/src/features/dashboardFilters/FilterConfiguration/index.tsx:67-81`); the source only asserts the checkbox, not which field was selected (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:309-325`). This leaves the semantic target mapping under-asserted.
- A source comment says React strict mode must be disabled to avoid an error when reopening the filter (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:302-304`). There is no linked issue or explanation; the Playwright environment must not silently change application strict-mode settings to accommodate this test.
- The source's export comment says “get the fourth button,” but the implementation uses a test ID rather than a fourth index (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:381-384`). Treat the comment as stale.
- The old API slug test is suspicious: the v1 dashboard controller route class currently defines nested dashboard operations but no root GET (`packages/backend/src/controllers/dashboardController.ts:35-41`, `packages/backend/src/controllers/dashboardController.ts:103-150`), while active frontend retrieval is project-scoped v2. Do not preserve the old URL without confirming the intended compatibility contract.
- Slug lookup uses `.limit(1)`, optionally adds a project filter, and throws `Dashboard not found` when absent (`packages/backend/src/models/DashboardModel/DashboardModel.ts:887-927`). The project-scoped v2 endpoint is the deterministic choice for seed-project tests; an unscoped slug contract needs explicit justification.
- There are no SVG path assertions or canvas pixel interactions. ECharts is detected only through `.echarts-for-react` wrapper counts (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:30`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:407`).

## Coordination requirements

- Current tests share the seed project and admin. The active mutation test leaves a `Title` dashboard and fixed-name charts; the skipped filter/export tests would additionally leave `Title`/`Small` content if re-enabled. Repeated local runs and Cypress/Playwright dual runs accumulate content and can make text/search locators ambiguous.
- Existing Playwright configuration serializes its own tests (`fullyParallel: false`, `workers: 1`) but cannot serialize against a separately running Cypress process (`packages/e2e/playwright.config.ts:8-12`). Until unique naming and cleanup land, schedule this file under the seed-project mutation key and never dual-run its mutation test.
- Use a per-run suffix for every created dashboard/chart name and retain the created dashboard UUID immediately after creation. Cleanup with authenticated `DELETE /api/v2/projects/{projectUuid}/dashboards/{uuid}` in `finally`/`afterEach`; dashboard-owned saved queries have a dashboard foreign key with `ON DELETE CASCADE` for permanent deletion (`packages/backend/src/database/migrations/20230718093901_add_dashbord_uuid_to_chart_table.ts:3-11`). Because installations may enable dashboard soft delete (`packages/backend/src/services/DashboardService/DashboardService.ts:1938-1955`), confirm whether test cleanup needs a permanent purge to avoid retained rows.
- Never update/delete the seeded `Jaffle dashboard` or seeded saved charts. Read tests and temporary slug-filter tests can run concurrently once they use scoped visible locators, but view counters/query-cache activity remain shared.
- No new shared helper is justified. A target-local `openSeedDashboard`/readiness function is used only by this file; authentication, base URL, viewport, and storage state already exist globally (`packages/e2e/playwright.config.ts:16-49`, `packages/e2e/playwright/auth.setup.ts:1-25`).

## Exact port plan

1. Create only `packages/e2e/playwright/app/dashboard.spec.ts` for the three active Cypress tests. Import `SEED_PROJECT`, `expect`, and `test`; rely on existing admin storage state instead of recreating `cy.login()`.
2. Add target-local functions in that file only: one to open the dashboards list and select the exact visible `Jaffle dashboard` link, and one to wait for visible chart readiness/error absence. Do not create shared dashboard infrastructure for this single source.
3. Port `Should see dashboard` with role/text locators scoped to the dashboard grid. Scroll the specific below-fold visualization into view, then assert the two named titles, `Payments total revenue`, table headers, and chart readiness. Preserve the three-ECharts expectation only as a scoped locator count and document that five seeded chart tiles include big-number/table renderers.
4. Port `views underlying data...` by scoping to the tile titled `What's our total revenue to date?`, clicking its visible big-number/menu trigger, selecting the visible `View underlying data` menu item without force, and asserting exact labels inside the visible dialog.
5. Port the active creation workflow as one test because chart redirects and filter/target persistence are its subject. Give all created dashboard/chart/Markdown titles a unique run suffix; capture the created dashboard UUID immediately after the create redirect; scope every Add/Save/Apply action to the active visible container; wait for exact combobox options instead of 200 ms sleeps; select filter tabs/tile rows by accessible name; and wait for URL transitions before Save clicks.
6. Wrap the mutable test in cleanup that deletes/purges only the captured dashboard UUID. Verify whether the configured soft-delete path also removes dashboard-owned charts; if not, use an approved permanent-cleanup API/test utility. Keep the suite in the serial seed-project lane until this cleanup is proven under failure and dual-run.
7. Do not mechanically copy the six `it.skip` blocks. Resolve them explicitly:
   - Add the slug route/render, slug temporary-filter reload, and invalid-slug rendered-error cases to `packages/e2e/playwright/app/dashboard.spec.ts` only if product owners confirm they should be revived.
   - Move the direct slug HTTP contract to `packages/api-tests/tests/dashboard.test.ts`, using the project-scoped v2 route unless v1 compatibility is explicitly required.
   - Move image-export job orchestration to `packages/api-tests/tests/dashboard.test.ts` if only job success matters; retain a small Playwright modal test only if modal/preview rendering is the actual contract and the scheduler/headless-browser/storage services are available.
   - Reclassify the “filters clear for new dashboards” skip as Playwright unless it is redesigned into focused DashboardProvider/router unit coverage; its current reload/navigation contract cannot be preserved by an API test.
8. Remove the Cypress source only in the migration change that has passed the Playwright/API replacements and after the dual-run mutation lane is no longer needed.

## Verification plan

Run from repository root, with the existing dev stack and seeded demo warehouse available:

```bash
pnpm -F e2e typecheck:playwright
pnpm -F e2e lint
pnpm -F e2e format
pnpm -F e2e playwright:run -- playwright/app/dashboard.spec.ts --project=firefox
```

For any API-test extraction from skipped cases:

```bash
pnpm -F api-tests typecheck:fast
pnpm -F api-tests lint
pnpm -F api-tests format
pnpm -F api-tests test:api -- tests/dashboard.test.ts
```

Before declaring dual-run safety, run the mutable Playwright test twice sequentially and once concurrently with the old Cypress source in an isolated disposable preview, then verify no visible `Title`/`Small` dashboards or dashboard-owned charts remain. Do not perform that destructive/shared-state verification during discovery.

## Open questions

- Why are all six tests skipped? The only evidence is repeated `todo: move to unit tests` comments (`packages/e2e/cypress/e2e/app/dashboard.cy.ts:38`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:351`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:389`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:410`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:452`, `packages/e2e/cypress/e2e/app/dashboard.cy.ts:473`); there is no linked issue, failure reason, or removal decision.
- Is unscoped `GET /api/v1/dashboards/{slug}` still a supported compatibility endpoint? The current frontend and v2 controller use the project-scoped route, and the v1 controller has no root GET action.
- Should invalid slug behavior assert a rendered `Dashboard not found`, a generic error page, or only an HTTP 404? The current skipped test does not distinguish those contracts.
- Is the React strict-mode warning at `packages/e2e/cypress/e2e/app/dashboard.cy.ts:303` still reproducible in the current app/Firefox project, and what is its root cause?
- When soft delete is enabled, what approved test-only cleanup permanently removes the created dashboard and its dashboard-owned charts? A normal dashboard DELETE can retain soft-deleted rows.
- For cross-explore filter targeting, should the test assert the actual auto-selected `Stg payments` field rather than only a checked checkbox?
- Is image-preview coverage expected in the normal E2E lane with scheduler, headless browser, and S3/MinIO, or should it live in a dedicated external-service lane?

## Port history

Not started.

### 2026-07-20 — Playwright port implemented (static-only)

- Target: `packages/e2e/playwright/app/dashboard.spec.ts`.
- Ported exactly the three active browser contracts: seeded dashboard rendering, the underlying-data dimensions/metrics dialog, and one atomic dashboard creation workflow covering a linked saved chart, three dashboard-owned charts across two explores, a persistent default filter, named tile targeting, and Markdown content.
- Skipped decisions: all six Cypress `it.skip` bodies remain unported and inactive; no API replacement files were added.
- Mutation/state safety: only the creation workflow is tagged `@mutating`; every created name has a per-run UUID suffix; response listeners and route parsing retain the created dashboard UUID and every dashboard-owned chart UUID; `finally` soft-deletes exact UUIDs, verifies exact dashboard/chart GETs return 404, and falls back to exact chart soft-delete plus a second 404 verification when dashboard cascade cleanup does not make a chart inactive. Inactive tombstones are intentionally retained.
- Static evidence:
  - `pnpm -F e2e typecheck:playwright` — passed.
  - `pnpm -F e2e linter ./playwright/app/dashboard.spec.ts` — passed with 0 errors/warnings.
  - `pnpm -F e2e formatter ./playwright/app/dashboard.spec.ts --check` — passed (`All matched files use the correct format`).
  - `git diff --check` — passed with no output.
  - Pre-history scope check confirmed the only implementation change was `packages/e2e/playwright/app/dashboard.spec.ts`; this finding is the only planned append.
  - Final scope/diff checks passed: `git diff --check`; an exact sorted-path comparison; and `git diff --no-index --check /dev/null packages/e2e/playwright/app/dashboard.spec.ts`. Final status contained only this modified finding and the untracked target spec.
- Live execution: deliberately not run. No Playwright browser, Cypress, or API mutation command was executed without the sole mutation lease.
- Remaining risks: Firefox/Mantine portal behavior, structural dashboard-header/filter-container scoping, virtualized explore/field locators, query/render timing, response payload assumptions, and soft-delete cascade/fallback behavior remain unverified until leased live execution. A hard Playwright timeout could still dispose fixtures while `finally` is running; the response listener minimizes UUID-loss risk but cannot prove teardown behavior statically.
- Commit: `<pending>`.

### 2026-07-20 — Execution lease evidence

- The first leased invocation used the package-script form before the direct-command clarification: `pnpm -F e2e playwright:run -- playwright/app/dashboard.spec.ts --project=firefox`. It selected exactly setup plus the three target tests, exposed locator mismatches, and was not used again. All subsequent Playwright gates used `pnpm -F e2e exec playwright test ...` directly.
- Root-cause fixes from Firefox snapshots: dashboard-list links include metadata in their accessible names; Mantine dialogs have no accessible dialog name; the saved-chart search is the exact named searchbox rather than a placeholder; selected explore fields move into `selected-field-*`; dashboard tiles render lazily and need named-tile scrolling; filter chips contain nested same-name buttons; the success alert copy is `Success! Dashboard was updated.`; and Markdown editor title controls require an exact textbox role. The seeded table contract remains a table-tile-scoped `thead th` count.
- Failure cleanup audits: every failed mutable attempt left zero active run-unique names. Exact dashboard tombstones audited by GET 404 were `bd6ff947-5305-4b5c-9975-7c09a0115623`, `daedcab3-8c12-42a0-bcc0-f8f760235f87`, `613bfc3d-f7b4-44ee-a803-97076a96b116`, `6efdbac7-2bed-4e4a-a768-612fafe89881`, `07a1b02e-3536-48c2-a18b-e450cdeb3c09`, `20f0b1f1-8775-4869-8905-2b8a83ce083f`, `d803336a-2374-48f8-be31-1f6c33cd32bc`, and `ef10338d-48ae-4e61-9bc1-80c6fdea91b4`. Exact owned-chart GET 404 UUIDs were `f1376877-da54-4764-9fbd-2a4bf69ae690`, `4533cf2e-2db6-4702-84bb-9f0ea1de098f`, `6097734f-cd29-4892-825f-f35ee6e6f37b`, `abb20a00-ec58-4eec-a427-979dac201bb1`, `aeac9a62-e917-4d87-b589-a7c828d8c08e`, `382a8d5d-6e7a-43de-bc6b-b1f4e98850c4`, `68e6a5ba-1436-447b-9276-d4b8a630ad87`, `8429209b-89aa-4070-a907-7dd0d56d6041`, `e2b0ddf8-274f-4171-962d-ae3add4261aa`, and `1397480d-7b10-4503-bd67-f798b5068040`. Before the first green gate the expected totals were eight dashboard tombstones and ten owned-chart tombstones, with zero active rows.
- Focused gate: `pnpm -F e2e exec playwright test playwright/app/dashboard.spec.ts --project=firefox` — 4 passed in 38.8s. Post-gate exact GET 404: dashboard `744815c8-a04c-4b26-88b5-ccc5e7d28265`; charts `4459ce2e-6888-42cb-ae8c-96451d58c51a`, `c0b232d2-15f4-47b8-8d4d-71eafb9e4e62`, and `f180814f-5fd6-4473-ba51-5e68d07ce5cd`; zero active run-unique names.
- Repeat gate: `pnpm -F e2e exec playwright test playwright/app/dashboard.spec.ts --project=firefox --repeat-each=3` — 10 passed in 1.9m. Exact GET 404 dashboards: `36b8a15f-a039-4e3d-8ee4-e60ce990e3dd`, `b48fe070-4143-4b2d-a0e2-d4d02ba69d3c`, `fc6e2b20-777d-498b-a192-2a9b8441c954`; charts: `ab2a5a0c-a65e-47ea-9a23-065abbecb189`, `b5e8d656-d0a2-43bf-a1b0-337e87351af5`, `bab7ed61-2dd7-4fdd-914a-8b479891c169`, `ee9e4583-2a65-485d-9871-0e644afb0d74`, `ef9626a0-bfe9-47d5-aea0-b619168fb356`, `f4c3d9d7-358b-494d-acfe-92d83b189b7b`, `f51b2b87-dd54-4e83-9eae-84cb7b7f3b51`, `f5f4e536-0d56-4729-8572-72c5e25aa927`, and `f7df756e-d5f7-4a99-8b36-d03dd6e44aa1`; zero active run-unique names; cumulative expected tombstones: 12 dashboards and 22 owned charts.
- Explicit mutable gate: `pnpm -F e2e exec playwright test playwright/app/dashboard.spec.ts --project=firefox --grep @mutating` — 2 passed in 31.7s. Post-gate exact GET 404: dashboard `384e8cb4-4768-4021-9193-523f4ad206bb`; charts `058a1067-636f-4590-97fe-9168e36ac7f6`, `1ec04349-1df2-433f-bed6-655efcb6e67e`, and `86a4b73a-21e7-4b19-8825-cb0e187287e3`; zero active run-unique names.
- Full branch Firefox gate: `pnpm -F e2e exec playwright test --project=firefox` — 9 passed in 1.2m. Post-gate exact GET 404: dashboard `8ac86fae-0e1d-4ab8-ab7a-892d6b433de6`; charts `29e96751-bc66-4af9-8a2e-c168d576c0b9`, `58569484-4a5e-4ae5-8f65-c5f33a1f8a3f`, and `b8c90f0e-2ca8-4a88-8977-82ba2d138469`; zero active run-unique names. Final expected Playwright tombstones: 14 dashboards and 28 owned charts.
- Legacy dual-run gate: pre-run active baselines were zero for dashboard name `Title` and the three fixed chart names. `pnpm -F e2e exec cypress run --e2e --browser firefox --spec cypress/e2e/app/dashboard.cy.ts` — 3 passing and 6 pending in 29s. Exact post-baseline deltas were dashboard `62d4937d-13fd-47b3-a74b-f8bc6e835d22` and charts `3da2b983-edf7-4984-8636-6b11f303ac0e`, `60d694de-96c9-464f-bcff-b27a645b534a`, and `df13f21a-7d7a-4cd5-b6b8-d1a63a9e2f38`. Only that dashboard delta was soft-deleted; its three exact chart deltas cascaded inactive; all four exact GETs returned 404. Final fixed-name state: zero active dashboards/charts and the expected one dashboard plus three chart tombstones. No seeded or baseline UUID was modified.
- Final static evidence: `pnpm -F e2e typecheck:playwright`, targeted ESLint, and targeted oxfmt check all passed after formatting.
- Final scope evidence: `git diff --check`, the untracked-spec `git diff --no-index --check`, and an exact sorted-path comparison all passed. `git status --short` contained only this modified finding and untracked `packages/e2e/playwright/app/dashboard.spec.ts`.
- Remaining risks: inactive tombstones intentionally remain (14 Playwright dashboards/28 owned charts plus one legacy dashboard/three legacy charts). Cleanup is proven for normal assertions and the exercised failures, but a hard runner process kill can still prevent JavaScript `finally` from running. The six legacy skipped contracts remain intentionally unported.
- Commit proposal: `chore(e2e): port dashboard tests to Playwright`.
