{
    "fields": [
        {
            "name": "groups",
            "label": "Grouping fields",
            "type": "dimension",
            "required": true,
            "multiple": true,
            "description": "Choose grouping columns in the order they should appear."
        },
        {
            "name": "values",
            "label": "Measures",
            "type": "metric",
            "required": true,
            "multiple": true,
            "description": "Choose measures in the order they should appear."
        }
    ],
    "configOptions": [
        {
            "name": "title",
            "label": "Title",
            "type": "text",
            "default": "Measures by group"
        }
    ],
    "colorPalette": null,
    "inputGuidance": "Use one row per group with numeric measures. The selected order controls the displayed columns."
}
