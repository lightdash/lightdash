{
  "schemaVersion": "2",
  "version": "2.234.0",
  "previousVersion": "2.233.0",
  "releaseDate": "2026-09-16T11:43:57.595Z",
  "migrations": {
    "present": true,
    "count": 1,
    "coreCount": 1,
    "eeCount": 0,
    "files": [
      {
        "name": "20260916100000_create_document_access_tables.ts",
        "edition": "core",
        "tables": [
          "document_group_access",
          "document_user_access",
          "documents",
          "groups",
          "users"
        ],
        "heaviness": {
          "locksTable": true,
          "rewritesTable": false,
          "scansTable": true
        }
      }
    ]
  },
  "compatibility": {
    "rollingUpdateSafe": true,
    "recommendedStrategy": "RollingUpdate"
  },
  "api": {
    "rest": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [
        "GET /api/v1/dashboards/{dashboardUuidOrSlug}/version/{versionUuid} — added the new `document` enum value to the `results/allOf[subschema #2]/dashboard/access/items/grantedVia` response property for the response status `200`",
        "PATCH /api/v1/projects/{projectUuid}/dashboards — added the new `document` enum value to the `results/items/access/items/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/dashboards — added the new `document` enum value to the `results/access/items/grantedVia` response property for the response status `201`",
        "POST /api/v1/projects/{projectUuid}/dashboards/with-charts — added the new `document` enum value to the `results/access/items/grantedVia` response property for the response status `201`",
        "PATCH /api/v1/projects/{projectUuid}/favorites — added the new `document` enum value to the `results/contentType` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/review-requests — added the new `document` enum value to the `results/data/items/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/review-requests — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequestListItem]/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `201`",
        "GET /api/v1/projects/{projectUuid}/review-requests/content/{contentType}/{contentUuid} — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/review-requests/{requestUuid} — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequestListItem]/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/review-requests/{requestUuid}/approve — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequestListItem]/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/review-requests/{requestUuid}/cancel — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequestListItem]/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/review-requests/{requestUuid}/reject — added the new `document` enum value to the `results/allOf[#/components/schemas/ContentReviewRequestListItem]/allOf[#/components/schemas/ContentReviewRequest]/grantedPrincipals/items/resourceType` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/spaces — added the new `document` enum value to the `results/items/allOf[subschema #2]/userAccess/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/spaces — added the new `document` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/spaces/{spaceUuid} — added the new `document` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "PATCH /api/v1/projects/{projectUuid}/spaces/{spaceUuid} — added the new `document` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/spaces/{spaceUuid}/access — added the new `document` enum value to the `results/data/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/sqlRunner/saved/slug/{slug} — added the new `document` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/sqlRunner/saved/{uuid} — added the new `document` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/sqlRunner/saved/{uuid}/promote — added the new `document` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "GET /api/v1/saved/{chartUuid}/version/{versionUuid} — added the new `document` enum value to the `results/chart/access/items/grantedVia` response property for the response status `200`",
        "POST /api/v2/content/bulk-action/{projectUuid}/delete — added the new `document` enum value to the `results/skipped/items/contentType` response property for the response status `200`",
        "GET /api/v2/projects/{projectUuid}/dashboards/{dashboardUuidOrSlug} — added the new `document` enum value to the `results/access/items/grantedVia` response property for the response status `200`",
        "PATCH /api/v2/projects/{projectUuid}/dashboards/{dashboardUuidOrSlug} — added the new `document` enum value to the `results/access/items/grantedVia` response property for the response status `200`",
        "GET /api/v2/projects/{projectUuid}/saved/{chartUuidOrSlug} — added the new `document` enum value to the `results/access/items/grantedVia` response property for the response status `200`"
      ],
      "advisoryCount": 25
    },
    "mcp": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [],
      "advisoryCount": 0
    }
  },
  "config": {
    "checked": true,
    "breaking": false,
    "changes": []
  },
  "upgrade": {
    "minPreviousVersion": "1.111.0",
    "requiredStops": []
  },
  "declaredBreaks": []
}
