{
  "schemaVersion": "2",
  "version": "2.41.0",
  "previousVersion": "2.40.0",
  "releaseDate": "2026-08-27T19:04:01.152Z",
  "migrations": {
    "present": false,
    "count": 0,
    "coreCount": 0,
    "eeCount": 0,
    "files": []
  },
  "compatibility": {
    "rollingUpdateSafe": true,
    "recommendedStrategy": "RollingUpdate"
  },
  "api": {
    "rest": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [
        "GET /api/v1/dashboards/{dashboardUuidOrSlug}/version/{versionUuid} — added the new `app` enum value to the `results/allOf[subschema #2]/dashboard/access/items/grantedVia` response property for the response status `200`",
        "PATCH /api/v1/projects/{projectUuid}/dashboards — added the new `app` enum value to the `results/items/access/items/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/dashboards — added the new `app` enum value to the `results/access/items/grantedVia` response property for the response status `201`",
        "POST /api/v1/projects/{projectUuid}/dashboards/with-charts — added the new `app` enum value to the `results/access/items/grantedVia` response property for the response status `201`",
        "GET /api/v1/projects/{projectUuid}/spaces — added the new `app` enum value to the `results/items/allOf[subschema #2]/userAccess/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/spaces — added the new `app` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/spaces/{spaceUuid} — added the new `app` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "PATCH /api/v1/projects/{projectUuid}/spaces/{spaceUuid} — added the new `app` enum value to the `results/access/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/spaces/{spaceUuid}/access — added the new `app` enum value to the `results/data/items/allOf[#/components/schemas/SpaceAccess]/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/sqlRunner/saved/slug/{slug} — added the new `app` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "GET /api/v1/projects/{projectUuid}/sqlRunner/saved/{uuid} — added the new `app` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "POST /api/v1/projects/{projectUuid}/sqlRunner/saved/{uuid}/promote — added the new `app` enum value to the `results/space/userAccess/grantedVia` response property for the response status `200`",
        "GET /api/v1/saved/{chartUuid}/version/{versionUuid} — added the new `app` enum value to the `results/chart/access/items/grantedVia` response property for the response status `200`",
        "GET /api/v2/projects/{projectUuid}/dashboards/{dashboardUuidOrSlug} — added the new `app` enum value to the `results/access/items/grantedVia` response property for the response status `200`",
        "PATCH /api/v2/projects/{projectUuid}/dashboards/{dashboardUuidOrSlug} — added the new `app` enum value to the `results/access/items/grantedVia` response property for the response status `200`",
        "GET /api/v2/projects/{projectUuid}/saved/{chartUuidOrSlug} — added the new `app` enum value to the `results/access/items/grantedVia` response property for the response status `200`"
      ],
      "advisoryCount": 16
    },
    "mcp": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [],
      "advisoryCount": 0
    }
  },
  "config": {
    "checked": true,
    "breaking": false,
    "changes": []
  },
  "upgrade": {
    "minPreviousVersion": "1.111.0",
    "requiredStops": []
  },
  "declaredBreaks": []
}
