{
  "schemaVersion": "2",
  "version": "2.281.0",
  "previousVersion": "2.280.0",
  "releaseDate": "2026-09-21T15:39:35.904Z",
  "migrations": {
    "present": true,
    "count": 1,
    "coreCount": 1,
    "eeCount": 0,
    "files": [
      {
        "name": "20260918120000_set_saved_merge_schema_default_v3.ts",
        "edition": "core",
        "tables": [],
        "heaviness": {
          "locksTable": false,
          "rewritesTable": false,
          "scansTable": false
        }
      }
    ]
  },
  "compatibility": {
    "rollingUpdateSafe": false,
    "recommendedStrategy": "Recreate"
  },
  "api": {
    "rest": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [
        "POST /api/v1/projects/{projectUuid}/charts/{slug}/code — removed `#/components/schemas/SavedMergeQuery` from the `allOf[#/components/schemas/Omit_ChartAsCode.chartConfig-or-description_]/merge` request property `allOf` list",
        "POST /api/v1/projects/{projectUuid}/code/charts/{slug} — removed `#/components/schemas/SavedMergeQuery` from the `allOf[#/components/schemas/Omit_ChartAsCode.chartConfig-or-description_]/merge` request property `allOf` list",
        "POST /api/v1/projects/{projectUuid}/dashboards/with-charts — removed `#/components/schemas/SavedMergeQuery` from the `charts/items/anyOf[#/components/schemas/CreateChartInDashboard]/allOf[#/components/schemas/CreateChartBase]/merge` request property `allOf` list",
        "POST /api/v1/projects/{projectUuid}/dashboards/with-charts — removed `#/components/schemas/SavedMergeQuery` from the `charts/items/anyOf[#/components/schemas/CreateChartInSpace]/allOf[#/components/schemas/CreateChartBase]/merge` request property `allOf` list",
        "POST /api/v1/projects/{projectUuid}/review-requests/similar — removed `#/components/schemas/SavedMergeQuery` from the `chart/merge` request property `allOf` list"
      ],
      "advisoryCount": 5
    },
    "mcp": {
      "checked": true,
      "breaking": false,
      "changes": [],
      "breakingCount": 0,
      "advisories": [],
      "advisoryCount": 0
    }
  },
  "config": {
    "checked": true,
    "breaking": false,
    "changes": []
  },
  "upgrade": {
    "minPreviousVersion": "1.111.0",
    "requiredStops": []
  },
  "declaredBreaks": [
    {
      "id": "saved-chart-merge-schema-v3",
      "reason": "Saved chart API responses now return the named schema v3 definition under merge (queries, join, keys, sort and limit), replacing schema v2 sources and joinKey. Clients reading merge must adopt the new response shape. Existing schema v2 uploads remain accepted and stored rows are upgraded on read with their field ids preserved.",
      "requiredStop": false
    }
  ]
}
