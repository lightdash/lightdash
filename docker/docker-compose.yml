version: "3.9"
services:
  lightdash-dev:
    build:
      context: ..
      dockerfile: dockerfile
      target: dev
    depends_on: 
      - db-dev
    environment:
      - LIGHTDASH_DB_HOST=db-dev
      - LIGHTDASH_DB_PORT=5432
      - LIGHTDASH_DB_DATABASE=postgres
      - LIGHTDASH_DB_USER=postgres
      - LIGHTDASH_DB_PASSWORD=password
      - POSTGRES_HOST=db-dev
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=postgres
      - POSTGRES_PORT=5432
      - POSTGRES_DATABASE=postgres
    volumes:
      - "../examples/full-jaffle-shop-demo/profiles:/usr/app/profiles"
      - "../examples/full-jaffle-shop-demo/dbt:/usr/app/dbt"
      - "../packages:/usr/app/packages"
    ports:
      - 3000:3000
      - 8080:8080

  db-dev:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
