# Agent ${AGENT_ID} environment — auto-generated by launch.sh
# Do not edit manually; re-run launch.sh to regenerate.

PORT=${API_PORT}
SITE_URL=http://localhost:${FE_PORT}
DOCKERFILE_TARGET=dev

PGHOST=localhost
PGPORT=${DB_PORT}
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=agent_${AGENT_ID}

SECURE_COOKIES=false
TRUST_PROXY=false
LIGHTDASH_SECRET=agent-${AGENT_ID}-secret

# Analytics / telemetry disabled for agents
RUDDERSTACK_WRITE_KEY=
RUDDERSTACK_DATA_PLANE_URL=

DBT_DEMO_DIR=${REPO_ROOT}/examples/full-jaffle-shop-demo
NODE_ENV=development
LIGHTDASH_MODE=development
LIGHTDASH_LOG_LEVEL=debug
LIGHTDASH_QUERY_MAX_LIMIT=5000

AUTH_GOOGLE_OAUTH2_CLIENT_ID=
AUTH_GOOGLE_OAUTH2_CLIENT_SECRET=
AUTH_DISABLE_PASSWORD_AUTHENTICATION=false

ALLOW_MULTIPLE_ORGS=false
DBT_PROJECT_DIR=.

SCHEDULER_ENABLED=false
GROUPS_ENABLED=true
EXTENDED_USAGE_ANALYTICS=false

INTERCOM_APP_ID=

HEADLESS_BROWSER_HOST=localhost
HEADLESS_BROWSER_PORT=${BROWSER_PORT}
INTERNAL_LIGHTDASH_HOST=http://localhost:${API_PORT}

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_DEFAULT_BUCKETS=agent-${AGENT_ID}
MINIO_BROWSER=off

# S3 (pointing at local MinIO)
S3_ENDPOINT=http://localhost:${MINIO_PORT}
S3_REGION=us-east-1
S3_BUCKET=agent-${AGENT_ID}
S3_SECRET_KEY=minioadmin
S3_ACCESS_KEY=minioadmin
S3_FORCE_PATH_STYLE=true

DEV_SSH_PUBLIC_KEY=

# Prometheus disabled for agents
LIGHTDASH_PROMETHEUS_ENABLED=false

# External services disabled
SENTRY_DSN=
SENTRY_SPOTLIGHT=
HEADLESS=true
