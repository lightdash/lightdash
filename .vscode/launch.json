{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Lightdash Backend (Docker)",
      "type": "node",
      "request": "attach",
      "address": "localhost",
      "port": 9229,
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/usr/app",
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
